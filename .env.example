# Connexion au GMS de DataHub (facultatif — l'application fonctionne en repli local).
# Une fois définies, l'agent lit la fraîcheur des sources dans le graphe DataHub,
# écrit l'état de ses runs et crée/résout des incidents (panne de station).
DATAHUB_GMS_URL=
DATAHUB_TOKEN=
